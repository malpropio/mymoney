development:
  allowed_users: dev@local.com, dev2@local.com

test:
  allowed_users: dev@local.com

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  allowed_users:
