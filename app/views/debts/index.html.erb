<p id="notice"><%= notice %></p>

<h1>Listing Debts/Assets</h1>

<table>
  <thead>
    <tr>
      <th>Category</th>
      <th>Old Category</th>
      <th>Sub category</th>
      <th>Name</th>
      <th>Payment Account</th>
      <th>Account</th>
      <th>Fix Amount</th>
      <th>Schedule</th>
      <th>Payment Start Date</th>
      <th>Auto Pay</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @debts.each do |debt| %>
      <tr>
        <td><%= debt.category ? debt.category.name : "None" %></td>
        <td><%= debt.old_category %></td>
        <td><%= debt.sub_category %></td>
        <td><%= debt.name %></td>
        <td><%= debt.pay_from %></td>
        <td><%= debt.account %></td>
        <td><%= number_to_currency(debt.fix_amount) %></td>
        <td><%= debt.schedule %></td>
        <td><%= debt.payment_start_date %></td>
        <td><%= debt.autopay %></td>
        <td><%= link_to 'Show', debt %></td>
        <td><%= link_to 'Edit', edit_debt_path(debt) %></td>
        <td><%= link_to 'Destroy', debt, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Debt', new_debt_path %>
